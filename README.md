# Initial dispatcher configuration

For a local setup which entails the least amount of editing layout the files thus:


    /etc/apache2/       -- this may vary depending on the OS
        http.conf
        conf/
            dispatcher.any
        conf.d/
            dispatcher.conf

